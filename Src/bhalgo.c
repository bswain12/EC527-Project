#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/* Useful for optimization later */

//#include <omp.h>
//#include <pthread.h>

/* We need to create a quadtree structure occupied by "nodes" or "points" which individually have
x and y coordinates and a Mass quantity. */
struct node {


};